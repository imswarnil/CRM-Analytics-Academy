---
title: "セキュリティユーザー & 列レベルのセキュリティ CRM Analytics"
navigation:
  title: "セキュリティユーザー"
description: "何かを学ぶ CRM Analytics セキュリティ ユーザは: レシピ内のデータをプレビューし、ユーザーオブジェクトフィールドを読み込み、セキュリティ述語で行レベルのセキュリティを推進します。"
video:
  id: "0UrasqsdHXU"
  start: 233
  end: 315
---

# セキュリティユーザー & 行レベルのセキュリティ

インテグレーションユーザーに加えて、 CRM Analytics 第2の特別なアカウントを出荷: **セキュリティユーザー**お問い合わせ インテグレーションユーザーのように、狭くて重要な仕事をしています。削除しないでください。 このレッスンでは、その2つの役割を説明し、プラットフォームの最も強力なアイデアの1つを紹介します。 **行レベルのセキュリティ**お問い合わせ

## セキュリティユーザーの2つのジョブ

セキュリティ ユーザーは2つのことを正確にするために存在します。

::lesson-cards
---
columns: 2
items:
  - title: "レシピのプレビューデータ"
    icon: "i-lucide-eye"
    description: "データセットを作成するときに「ビュー as」データプレビューを出力します。 Data Prep レシピなので、データを変換する際にサンプル行を見ることができます。"
  - title: "述語のユーザーオブジェクトフィールドを読みます"
    icon: "i-lucide-users"
    description: "クエリ時にユーザーオブジェクトのフィールドを読み込み、セキュリティ述語は各実行中のユーザーがどの行が表示されるかを判断できます。"
---
::

2番目のジョブは、行レベルのセキュリティ作業を行うため、深く理解する価値があります。

::note{icon="i-lucide-shield-alert"}
**セキュリティユーザーを削除または無効にしないでください** お問い合わせ 述語が依存しているユーザーフィールドへのアクセスを失うと、行レベルのセキュリティはサイレントブレイクでき、ユーザーはあまりにも多くの行を見ることができます。
::

## 実際に行レベルのセキュリティとは

同じ瞬間に2人の人が同じダッシュボードを開きます。 **お問い合わせ** 実行してみる **1,000行**;;; **お問い合わせ** 実行してみる **2,000行**お問い合わせ ダッシュボードが変更されたことは何もありません。 *スタッフ* 実行中です。 です。 **行レベルのセキュリティ**: プラットフォームは、ユーザーごとにデータセットの行をフィルタリングします。

背後にある機構は **セキュリティの述語** — 評価されるデータセットに添付されたフィルタルール **クエリ時間** 実行中のユーザーに対して。 述語は「レコードの所有者が実行中のユーザーを等しくする行のみを表示する」や「実行中のユーザー領域の行のみを表示する」といったようなことを言うことができます。 クエリ時に実行されるため、同じデータセットは異なるスライスを自動的に異なる人々に提供します。

::tip{icon="i-lucide-filter"}
セキュリティ述語は、実行中のすべてのクエリで新鮮に評価されます。 そのため、1つのデータセットは、別々のデータセットを作成せずに、自分のアカウントと全体の領域を見ることができるVPだけを見て、安全に担当者にサービスを提供することができます。
::

## セキュリティの場所 ユーザは

述語を評価するため、 CRM Analytics 属性の読み込み **実行ユーザ** — ユーザ ID、ロール、地域、チーム、その他のフィールド **ユーザーオブジェクト**お問い合わせ これらのフィールドを読み込む **セキュリティユーザーとして**お問い合わせ つまり、Security User が User オブジェクトにアクセスする必要があるのは、まさに「誰がこの人であり、その属性は何ですか?」と表示されるアカウントなので、述語はそれに応じてフィルタリングできます。

## カスタムユーザーフィールドのgetcha

述語を作成できます **ユーザーオブジェクト上の任意のフィールド** — 標準的な分野のような `UserId` そして、 `Role`またはあなた自身の **カスタム ユーザーフィールド** (say、カスタム) `Region__c` または `Team__c`)。 しかし、インテグレーションユーザーをミラーリングするキャッチがあります。

::note{icon="i-lucide-triangle-alert"}
あなたの述語が参照する場合 **ユーザーオブジェクトのカスタムフィールド**, , **セキュリティ ユーザはアクセスを読まれなければならない** そのフィールドに。 これを見逃し、述語は値を読むことができません。行レベルのセキュリティは正しくフィルタリングしません。
::

## 2つの特別なユーザー、1パターン

このレッスンと最後のレッスンで対称性をお伝えください。 インテグレーションユーザーとセキュリティユーザーの両方が、あなたが生き続ける必要があるシステムアカウントであり、どちらも同じカスタムフィールドのgetchaを共有します。 **標準フィールドは自動的に機能しますが、カスタムフィールドには明示的な読み取りアクセスが必要です。** そのパターンと2つの設定バグのカテゴリ全体が消えます。

次に、org-level を見てみましょう **アナリティクスの設定** 機能のオンとオフを切り替える — そして、練習するための無料のorgを取得する方法.
